<div class="table-flow-list">
  <table class="table">
    <thead>
    <tr>
      {{#each netflowHeadings as |heading|}}
        <th>{{heading}}</th>
      {{/each}}
    </tr>
    </thead>
    <tbody>
     {{network-table data=tableData}}
    </tbody>
  </table>
</div>